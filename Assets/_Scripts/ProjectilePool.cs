using UnityEngine;
using System.Collections;

namespace LATwo
{
    public class ProjectilePool : Pool<ProjectileBehaviour>
    {
        //empty weirdchamp, just need this so i can use the generic pool monobehaviour
    }
}